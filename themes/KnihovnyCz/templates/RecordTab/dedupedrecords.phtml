<?php
  // Set page title.
  $this->headTitle($this->translate('Institutions') . ': ' . $this->driver->getBreadcrumb());
  $records = $this->driver->getDeduplicatedRecords();
?>
<?php if(!empty($records)): ?>
  <?php foreach ($records as $record): ?>
    <li>
      <a class="hover-overlay" href="<?=$this->recordLink()->getUrl($record['id'])?>">
        <?=$this->transEsc('Source::' . $record['source'])?>
      </a>
    </li>
  <?php endforeach; ?>
<?php endif;
