<?php foreach ($this->driver->getFormats() as $format): ?>
  <?php if ($translated = $this->transEscWithPrefix('Format::', $format)): ?>
    <span class="format <?=$this->record($this->driver)->getFormatClass($format) ?>"><?=$translated ?></span>
  <?php endif; ?>
<?php endforeach; ?>
