<?php foreach ($this->driver->getFormats() as $format): ?>
  <span class="format <?=$this->record($this->driver)->getFormatClass($format) ?>"><?=$this->transEscWithPrefix('Format::', $format) ?></span>
<?php endforeach; ?>
