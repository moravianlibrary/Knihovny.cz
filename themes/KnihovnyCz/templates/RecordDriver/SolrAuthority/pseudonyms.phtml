<?php foreach ($data as $author => $authRecordId):
  $escapedName = $this->escapeHtml($author); ?>
  <div>
    <a title="<?=$this->translate('Show person')?> <?=$escapedName?>" href="<?=$this->recordLink()->getUrl($authorityRecordId)?>"><i class="fa fa-user-circle-o"></i></a>
    <a title="<?=$this->translate('Show publications by')?> <?=$escapedName?>" href="<?=$this->record($this->driver)->getLink('author', $author)?>"><?=$escapedName?></a>
  </div>
<? endforeach;
